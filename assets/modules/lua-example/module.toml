id = "lua-example"
name = "Lua Example"
description = "Displays formatted local time from a package-local Lua script"
command = ["gisland-lua-host"]
entry = "example.lua"
config = "config.toml"
view = "view.toml"

[protocol]
major = 1
minimum_minor = 8
maximum_minor = 8

[options_schema.format]
type = "string"
